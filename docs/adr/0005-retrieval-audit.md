# ADR 0005: Retrieval records delivery, not influence

Status: Accepted

Each retrieval writes a request and item rows. Explain can show which retrievals returned a claim. It cannot claim that a subsequent agent action was influenced without a separately reported action-use link. Exact-scope deterministic retrieval is sufficient for Phase 1.

